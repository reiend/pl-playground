-- 1.5
-- What is the value of the expression type(nil) == nil?
-- (You can use Lua to check your answer.) Can you explain this result?

-- false
-- type(nil) returns a string value, which is truthy
-- nil is type of nil which is false
-- therefore true == false -> false
